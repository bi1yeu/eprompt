
## Usage

```sh
source .env              # .env should export MAILSERVER_USER and MAILSERVER_PASS
python journmail.py send # send reminder email to solicit journaling
python journamil.py read # read latest journal response and persist to journal file
```
